# Feature Flags

Feature flags allow turning features on/off without a redeploy.

## Usage

Feature flags are checked server-side before enabling functionality.

## Emergency Killswitch

If a feature causes issues:
1. Disable the flag in the admin dashboard
2. The feature stops immediately for all users
